<div class="jumbotron mx-auto" style="width:95%;">
  <div class="bg">
    <h1 class="display-4">BeoPLE</h1>
    <p class="lead">自宅で自分に合う商品を見つけませんか?</p>
    <hr class="my-4">
    <p>ファッション・メイクに特化した評価SNS</br>
      パーソナルカラー・骨格タイプを軸に使用感を共有します</p>
    <% if user_signed_in? %>
      <%= link_to(users_path, class: "button") do %>
        <span>TIME LINE</span>
      <% end %>
    <% else %>
      <%= link_to(new_user_registration_path, class: "button") do %>
        <span>使ってみる！</span>
      <% end %>
    <% end %>
  </div>
</div>
<div id="fh5co-main">
			<div class="fh5co-cards">
				<div class="container-fluid">
					<div class="row scrollanime">
						<div class="col-md-12 heading text-center"><h2>人気のREVIEW</h2></div>
					</div>
					<div class="row">
           <% @all_ranks.first(4).each do |review| %>
						<div class="col-lg-3 col-md-6 col-sm-6 scrollanime downup">
							<div class="fh5co-card">
							  <% review.review_images.first(1).each do |image| %>
                <%= attachment_image_tag image, :image, :fill, 200, 200, format: 'jpeg', fallback: "sample.png", class: "img-responsive" %>
                <% end %>
								<div class="fh5co-card-body">
									<h3 class="card-title"><%= review.item.name %></h3>
									<p><%= review.body %></p>
								</div>
							</div>
						</div>
						 <% end %>
					</div>
				</div>
			</div>
			<div class="animate-box fh5co-product-2 row">
				<div class="fh5co-half col-6">
				</div>
				<div class="fh5co-half col-6">
					<h3>テスターが不要な世界へ</h3>
					<p>気軽にタッチアップや試着の出来ない昨今<br>
					みなさんはどの様にメイク用品や洋服を購入していますか？<br>
					BeoPLEではパーソナルカラー・骨格診断を利用した利用満足度を共有します。
					共有することにより、試さずとも自身に1番似合う物をピックアップすることが可能となります。</p>
					<% if user_signed_in? %>
            <%= link_to(users_path, class: "button") do %>
              <span>TIME LINE</span>
            <% end %>
          <% else %>
            <%= link_to(new_user_registration_path, class: "button") do %>
              <span>使ってみる！</span>
            <% end %>
          <% end %>
				</div>
			</div>
			<div>
			  <h3>お問い合わせ</h3>
			</div>
			
			</div>
			
<script>
$(function () {
  $(window).scroll(function () {
      const wHeight = $(window).height();
      const scrollAmount = $(window).scrollTop();
      $('.scrollanime').each(function () {
          const targetPosition = $(this).offset().top;
          if(scrollAmount > targetPosition - wHeight + 60) {
              $(this).addClass("fadeInDown");
          }
      });
    });
});
</script>